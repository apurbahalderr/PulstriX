version: '3.8'

services:
  incident-dedup:
    build: ./incident_dedup
    ports:
      - "8001:8000"
    environment:
      - PORT=8000

  priority-classifier:
    build: ./priority_classification
    ports:
      - "8002:8000"
    environment:
      - PORT=8000

  text-dedup:
    build: ./text_dedup
    ports:
      - "8003:8000"
    environment:
      - PORT=8000
